## 一、规范概述

### 1.1 核心思想

二阶段晋升模式的核心是：**将“首轮推理轨迹锚定”与“完整工具能力解锁”分离**。

| 阶段                   | 目标                           | 工具面                 | 提示词风格                     |
| :--------------------- | :----------------------------- | :--------------------- | :----------------------------- |
| **一阶段（锚定）**     | 让模型进入稳定、高效的推理轨迹 | Minimal（2个核心工具） | 极简、对齐 Minimal             |
| **二阶段（晋升执行）** | 基于锚定轨迹执行完整任务       | Standard（全部工具）   | 继承锚定 persona，扩展任务指令 |

社区实测数据表明：Standard 预设首轮暴露 25 个工具，Project2 得分约 91/92；Minimal 预设首轮仅 1 行提示 + 2 个工具，得分约 99/96。二阶段方案在保留 Minimal 首轮优势的同时，后续解锁全部工具，得分可达 98/99。

## 二、晋升触发机制

### 2.1 触发条件

晋升由**首次持久信号**触发，社区标准配置为 `promoteOn: "either"`——即**首次 tool/call 或首次 assistant/message，谁先到谁晋升**。

若希望强制模型“先行动再晋升”，可配置为 `promoteOn: "tool-call"`，要求首轮必须调用工具才能触发晋升。

### 2.2 晋升状态的持久化

晋升状态写入**持久会话事件（durable session event）**，会话刷新或恢复后仍保持有效。

### 2.3 关键约束

- **首轮请求必须清空运行时上下文**——任何第三方插件、hook、runtime context 都不能污染首请求
- **首轮仅暴露 Minimal 的真实工具对**：`bash` + `str_replace_editor`（或 `read`）
- **晋升后 system prompt 保持不变**——社区实测表明，system prompt 突变（如 46→6620 字符）本身就是扰动源

## 三、提示词设计规范

### 3.1 一阶段提示词（锚定阶段）

**设计原则**：

1. **极简原则**：system prompt 保持 Minimal 原文（如 `You are a helpful software engineer assistant.`）
2. **任务匹配原则**：首条用户消息决定锚定类型

**锚定类型映射**：

| 任务类型                  | 锚定角色         | 首轮工具                  |
| :------------------------ | :--------------- | :------------------------ |
| **spec** — 修复/维护/调试 | Minimal 原版提示 | `bash` + `read` + `edit`  |
| **react** — 从零构建/创建 | 动手实践型提示   | `bash` + `read` + `write` |
| **weak** — 意图不明确     | 模型自行分类     | `bash` + `read`           |

> ⚠️ `glob`/`grep` 不出现在任何引导阶段的工具目录中

**一阶段提示词模板**：

markdown

```
【角色】{锚定角色描述，与任务类型匹配}
【目标】用一句话说清本次任务的核心交付物
【约束】
- 仅使用 {bash/read/edit/write} 进行最小化操作
- 禁止穷举式搜索，采用“由粗到细”的探索策略
【行动】请先执行 {最小锚定动作，如 ls -la}，完成后即可晋升调用完整工具链
```



### 3.2 二阶段提示词（晋升执行阶段）

**设计原则**：

1. **继承锚定 persona**：晋升后角色保持不变
2. **中性措辞**：晋升后指令提示采用“参考文档、按需查阅”的中性措辞，避免命令式（如 “read first and follow them”）——社区实测表明命令式会把锚定轨迹打回 `let me`
3. **按需解锁**：重型工具经 `dev_tool_search` 按需解锁，避免“全目录倾倒”

**二阶段提示词模板**：

markdown

```
【角色】继承一阶段锚定的角色 persona（保持不变）
【目标】基于一阶段探索/分析结果，执行深度任务
【方法】
1. {步骤一}
2. {步骤二}
3. {步骤三}
【参考】可参考项目文档，按需查阅（非强制）
【产出】{结构化输出要求}
```



### 3.3 完整示例：探索 + 排查场景

**一阶段（锚定探索）**：

markdown

```
【角色】你是一名资深系统架构师，正在进行项目健康度侦察。
【目标】快速建立对当前项目的全局认知，为后续深度安全审计做准备。
【约束】
- 仅使用 bash 和 read 工具进行只读操作，禁止任何修改
- 探索范围限定在项目根目录及其子目录下
- 禁止穷举式搜索，采用“由粗到细”的探索策略
【行动】请先执行 ls -la 查看根目录结构，完成探索后即可晋升调用完整工具链。
```



**二阶段（晋升执行）**：

markdown

```
【角色】继承一阶段架构师 persona。
【目标】基于上一阶段的探索报告，执行 P0 级安全隐患深度排查。
【方法】
1. 依赖链审计：深入分析依赖文件，识别已知高危漏洞
2. 凭证与密钥扫描：全局搜索硬编码的密码、API Key、Token
3. 权限与配置核查：检查关键配置文件和脚本的权限设置
【参考】可参考项目安全文档，按需查阅。
【产出】生成结构化的《P0 安全隐患排查报告》，按风险等级分类并给出修复建议。
```



## 四、配置参数规范

### 4.1 关键配置项

| 参数                     | 推荐值                  | 说明                        |
| :----------------------- | :---------------------- | :-------------------------- |
| `promoteOn`              | `either` 或 `tool-call` | 晋升触发条件                |
| `bootstrapMaxTokens`     | 1024                    | 首轮输出上限，防止过度分析  |
| `residentTools`          | 5-18 个核心工具         | 晋升后常驻工具集            |
| `includeSubagents`       | `true`                  | 子代理走同一套锚定流程      |
| `includeHarnessIdentity` | `true`                  | 是否包含 Harness 身份开场白 |

### 4.2 工具目录配置

晋升后工具目录采用**渐进暴露**策略：

1. **常驻工具集（resident）**：5-18 个核心工具，含 `memory`、`web_search`、`read_image`、`skill_search`、`skill_load` 等
2. **按需解锁**：当 Agent 确定需要专用工具时，通过 `dev_tool_search` 查询并加载

## 五、反模式与避坑指南

### ❌ 反模式一：首轮暴露过多工具

Standard 预设首轮暴露 25 个工具，导致 V4 Pro 首轮落到 “Let me…” 的 standard-like 轨迹。**应严格限制首轮工具为 2-3 个。**

### ❌ 反模式二：晋升后 system prompt 突变

晋升后 system prompt 从 46 字符突变为 6620 字符，本身就是扰动源。**应全程保持 Minimal 那一句 system prompt**。

### ❌ 反模式三：命令式 instruction-hint

“read first and follow them” 这类命令式措辞会把锚定轨迹打回 `let me`。**应采用“参考文档、按需查阅”的中性措辞**。

### ❌ 反模式四：首轮请求被污染

第三方插件、hook、runtime context 注入额外提示会污染首请求。**应在晋升前阻止所有自动注入。**

## 六、规范速查表

| 维度              | 一阶段（锚定）                     | 二阶段（晋升执行）             |
| :---------------- | :--------------------------------- | :----------------------------- |
| **System Prompt** | Minimal 原文（~46 字符）           | **相同**，保持不变             |
| **工具集**        | 2-3 个（bash + read ± edit/write） | 全部 Standard 工具（按需解锁） |
| **提示词风格**    | 极简、任务匹配                     | 继承 persona + 中性参考        |
| **上下文**        | 清空，零注入                       | 恢复 plan-mode 等段落          |
| **触发条件**      | 用户首条消息                       | 首次 tool/call 或 message      |
| **输出限制**      | `bootstrapMaxTokens: 1024`         | 无特殊限制                     |

------

本规范基于 DeepSeek Harness 社区 `dsh-anchored-standard` 等预设方案的实测经验整理，可根据具体任务类型（spec/react/weak）灵活调整锚定角色和首轮工具组合。
